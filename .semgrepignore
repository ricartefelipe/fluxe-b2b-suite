# Escopo do job Semgrep (OWASP Top Ten via semgrep.yml): código de aplicação em saas-suite-ui e scripts.
# O que está abaixo é revisto por outros meios (Terraform plan/tfsec, review de PR em GHA, hardening de nginx em deploy).

# Infrastructure as code (regras AWS/Terraform do pack geram bloqueios que não são bugs de app)
infra/

# Nginx de reverse proxy / static — configs de contentor, não superfície de ataque da SPA
deploy/nginx/

# Nginx mínimo por app (root estático)
**/nginx.conf

# Workflows: regras yaml.github-actions (ex.: run-shell-injection) são ruidosas em padrões comuns ${{ }}
.github/workflows/
